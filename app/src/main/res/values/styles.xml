<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Add splash theme but don't redefine Theme.DiceMaster -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/dark_wood_background</item>
    </style>
</resources>